# Leolist Sign Scanner

Railway-ready FastAPI scanner.

Environment variable required:

OPENAI_API_KEY=your_new_key

Deploy:
1. Upload this folder/ZIP to GitHub.
2. Connect Railway to the repo.
3. Set OPENAI_API_KEY in Railway Variables.
4. Railway should use the included Dockerfile.

Health check:
/health
